[pytest]
# Add project root to sys.path so local imports (config_loader, engines.*, ml.*)
# resolve correctly regardless of invocation directory.
pythonpath = .
testpaths = tests
addopts = --tb=short -q
